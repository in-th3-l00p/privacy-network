import React from "react";
// @ts-ignore
import {Container} from "react-bootstrap";

const Home = () => {
    return (
        <Container className={"bg"}>
        </Container>
    );
}

export default Home;